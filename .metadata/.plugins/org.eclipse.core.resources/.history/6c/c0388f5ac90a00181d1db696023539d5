package search;

import java.util.TreeMap;

import problem.State;

public class LocalSearchResultObject extends AResultObject {

	public LocalSearchResultObject(State goalState) {
		super(createMap(goalState));
	}
	
	private static TreeMap<String, Object> createMap(State goalState) {
		TreeMap<String,Object> map = new TreeMap<>();
		map.put(GOAL_STATE, goalState);
		return map;
	}
	
	private static final string GOAL_STATE = "GOAL_STATE";
	
}
