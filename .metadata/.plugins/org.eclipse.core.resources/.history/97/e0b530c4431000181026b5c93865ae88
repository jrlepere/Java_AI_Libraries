package csp_search;

import csp.CSP;
import csp.Domain;
import csp.Variable;

public interface SelectUnassignedVariable<X extends Variable<D, V>, D extends Domain<V>, V> {
	public V getVariable(List<X> assignment, CSP<X, D, V> csp);
}
